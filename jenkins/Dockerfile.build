FROM node:16-alpine3.16
RUN adduser --gecos "" --disabled-password jenkins \
    echo "jenkins:jenkins" | chpasswd \
    chown jenkins:jenkins /home/jenkins
USER jenkins
RUN apk add -U \
    python3\
    make\
    alpine-sdk

